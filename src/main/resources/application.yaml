ktor:
  deployment:
    port: 8080
  application:
    modules:
      - com.example.pluguns.SerializationKt.configureSerialization
      - com.example.pluguns.SecurityKt.configureSecurity
      - com.example.pluguns.RoutingKt.configureRouting
      - com.example.pluguns.DatabaseKt.configureDatabase

database:
  url: "jdbc:postgresql://localhost:5432/trackly_db"
  user: "postgres"
  password: "1234"
  driver: "org.postgresql.Driver"

jwt:
  secret: "trackly_super_secret_key"
  issuer: "trackly_server"
  audience: "trackly_users"
  realm: "Trackly"


kind: Application
